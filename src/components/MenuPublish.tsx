import { View, Text, TouchableOpacity } from "react-native";
import React from "react";
import { Button } from "native-base";

interface Props {
  title: string;
}

const ButtonPublish = ({ title }: Props) => {
  return (
<Button>
    
</Button>
  );
};

export default ButtonPublish;